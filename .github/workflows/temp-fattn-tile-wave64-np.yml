name: Temporary fattn tile wave64 np patch

on:
  push:
    branches:
      - feature_change_to_warp_size64

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature_change_to_warp_size64
          fetch-depth: 0
      - name: Apply patch
        run: python3 .github/scripts/temp-fattn-tile-wave64-np.py
      - name: Commit patch
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --check
          git commit -m "hip: combine GCN fattn tile np pairs in wave64"
          git push origin HEAD:feature_change_to_warp_size64
