name: Temporary fattn tile wave64 VKQ patch

on:
  push:
    branches:
      - feature_change_to_warp_size64

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature_change_to_warp_size64
          fetch-depth: 0
      - name: Fix patch script
        run: python3 .github/scripts/temp-fix-fattn-tile-wave64-vkq.py
      - name: Apply patch
        run: python3 .github/scripts/temp-fattn-tile-wave64-vkq.py
      - name: Commit patch
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git diff --cached --check
          git commit -m "hip: use wave64 VKQ hot loop on GCN D256"
          git push origin HEAD:feature_change_to_warp_size64
